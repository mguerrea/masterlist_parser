# masterlist_parser

```
usage : ./masterlist_parser [options] file
where options are
 --export-all         export all certificates in separate files
 --export-signer      export masterlist signer certificate
 --export-CSCA        export CSCA
 --print-infos        print countryName, commonName and date
 ```
